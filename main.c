#include <stdio.h>
#include "my_mat.h"

int main() {
    char choice = 0;

    while(choice != 'D')
    {
        scanf("%c", &choice);

        switch (choice)
        {
            case 'A':
                break;

            case 'B':
                break;

            case 'C':
                break;

            default:
                break;
        }
    }

    return 0;
}